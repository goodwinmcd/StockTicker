version: 0.2

phases:
    install:
        runtime-versions:
            dotnet: 5.0
    build:
        commands:
            - dotnet build -c Release ./StockTickerApi/StockTickerApi.csproj
    post_build:
        commands:
            - dotnet publish -c Release ./StockTickerApi/StockTickerApi.csproj
artifacts:
    files:
        - '**/*'
    base-directory: ./StockTickerApi/bin/Release/net5.0/publish/